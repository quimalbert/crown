import '../../domain/custom_card.dart';

class DeadCardFactory {
  List<CustomCard> call(Map<String, dynamic> _deadDialogMap) {
    List<CustomCard> _deadCardList = [];

    return _deadCardList;
  }
}
